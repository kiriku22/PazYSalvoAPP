@*@model IEnumerable<PazYSalvoAPP.WebApp.Models.ViewModels.EstadoViewModel>*@
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="container">
    <h2>Administrador de Roles</h2>
    <p class="descripcion">
        Este administrador nos permite visualizar y gestionar todo lo referentea roles del negocio.
    </p>

    <div class="opciones-container">
        <a class="btn btn-primary btn-sm" id="btnVerRoles">Ver Roles</a>
        <a class="btn btn-success btn-sm visually-hidden" id="btnNuevoRole">Nuevo Role</a>
    </div>

    <div id="tblListaDeRoles">
    </div>
</div>

@*MODAL*@
<div class="modal-container">
    <div class="modal" id="modal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="tituloModal"></h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form method="post" id="frmModalRole">
                    <div class="modal-body">

                        <div class="form-group">

                            <input type="hidden" class="form-control" id="IdValor">
                        </div>

                        <div class="form-group">
                            <label for="RoleValor">Nombre</label>
                            <input type="text" class="form-control" id="RoleValor">
                        </div>
    
                
                
                        <div class="form-group">
                            <label for="RoleValor">Descripción</label>
                            <input type="text" class="form-control" id="RoleValor">
                        </div>
                        <div class="form-group">
                            <label for="RoleValor">Activo</label>
                            <input type="text" class="form-control" id="RoleValor">
                        </div>

                        

                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                        <button type="submit" class="btn btn-primary" id="btnActualizarRole">Agregar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

</div>
@*FIN MODAL*@


@section Scripts {
    <script>
        $(document).ready(function () {
            
            // btnVerEstados
            $("#btnVerRoles").click(async function () {
                const url = "@Url.Action("ListarRoles", "Role")";
                const response = await $.ajax({
                    url: url,
                    method: "GET",
                    dataType: "html"
                });

                $("#tblListaDeRoles").html(response);
                $("#btnVerRoles").hide();
                $("#btnNuevoRole").removeClass("visually-hidden"); // visually-hidden

            });

            // mostrar modal nuevo estado
            $("#btnNuevoRole").click(function () {
                mostrarModal();


            });

        });

        function mostrarModal() {

            // Actualiza el título de la modal
            $("#tituloModal").text("Role");

            // Limpia los campos del formulario
            $("#frmModalRole").trigger("reset");

            // Muestra la modal
            $("#modal").modal("show");

        }
    </script>
}